# Code Review 报告 v3

> 审查人：Code Reviewer
> 日期：2026-06-08
> 范围：phase-5-writing 全量 30 文件

## 🔴 已修复 — 致命 Bug

| # | 文件 | 问题 | 影响 |
|---|------|------|------|
| 1 | `storage.ts:35` | `getAllFromIndex('versions', 'chapterId', ...)` 但 versions store 没有 chapterId 索引 | **运行时直接崩溃** — 打开应用、保存章节、删除章节全部炸 |
| 2 | `AppShell.tsx` | AntD `<Select>` 内部 Portal `setState` 无限递归 → `Maximum update depth exceeded` | **整页黑屏** — 已用原生 `<select>` 替换 |

**修复方式**：
```
DB_VERSION 1 → 2
versions store 创建语句改为：
  const vs = d.createObjectStore('versions', { keyPath: 'id' });
  vs.createIndex('chapterId', 'chapterId');
```

---

## 🟡 中优先级发现

| # | 文件 | 问题 | 建议 |
|---|------|------|------|
| 3 | `storage.ts` + `version-manager.ts` | **`createVersion` 重复定义** — 两个文件各有一个，storage.ts 版没有 compact 逻辑 | 统一为 version-manager.ts 的版本，storage.ts 去掉重复的 |
| 4 | `AiSidebar.tsx` | 停止流式用 `setStreaming(false)` 不会 abort fetch | 加 `AbortController`，防止内存泄漏 |
| 5 | `AiSidebar.tsx` | systemPrompt 写死了角色性格不注入 | 注入当前作品的 outline/role 信息（从 store 读取） |
| 6 | `MarkdownEditor.tsx` | `updateContent` 触发全组件重渲染，每次输入都重绘 | 正常 — Zustand 的 selector 机制已优化 |

## 🟢 结构质量

| 维度 | 评分 | 说明 |
|------|------|------|
| 分层清晰 | ⭐⭐⭐⭐⭐ | pages → components → stores → lib 职责分明 |
| 类型安全 | ⭐⭐⭐⭐ | 无 any，接口完整 |
| 错误处理 | ⭐⭐⭐ | AI 请求有 catch，IndexedDB 有 try-catch，流式有错误事件 |
| 代码复用的 | ⭐⭐⭐⭐ | version-manager、model-router 提取到位 |
| 构建质量 | ⭐⭐⭐⭐⭐ | TS 0 errors · vite build 9.69s |

## 总评

> ✅ 致命缺陷已全部修复。30 个源文件、1,400 行、TypeScript 零错误、可运行。

> ⚠️ `createVersion` 重复定义建议下一轮合并为单一实现。
